---
import AdminLayout from '@/layouts/AdminLayout.astro';
import AuthGuard from '@/components/admin/AuthGuard';
import AdminSidebar from '@/components/admin/AdminSidebar';
import TimelineManager from '@/components/admin/TimelineManager';
---

<AdminLayout title="Timeline Admin">
  <AuthGuard client:only="react" />
  <div class="flex h-screen overflow-hidden w-full">
    <aside class="w-64 shrink-0 z-50">
      <AdminSidebar currentPath="/admin/matthew/timeline" client:load />
    </aside>
    <main class="flex-1 overflow-y-auto p-8 min-w-0">
      <div class="max-w-6xl mx-auto w-full">
        <TimelineManager client:load />
      </div>
    </main>
  </div>
</AdminLayout>
